# Database Configuration
POSTGRES_USER=khvilon
POSTGRES_PASSWORD=mypass
POSTGRES_DB=ollamify
POSTGRES_PORT=5432

# AI Configuration
RERANKER_MODEL=qilowoq/bge-reranker-v2-m3-en-ru

# OpenRouter Configuration
OPENROUTER_API_KEY=sk-or-v1-98...
OPENROUTER_URL=https://openrouter.ai/api/v1/chat/completions

# Admin Configuration
JWT_SECRET=your-super-secret-key-change-in-production

# -----------------------------
# HTTPS / Nginx (www3)
# -----------------------------
# Domain name used for self-signed certificate generation and LetsEncrypt (optional).
# For local dev you can keep "localhost".
DOMAIN=localhost

# Host ports for the UI gateway (www3 / Nginx)
WWW_PORT_HTTP=80
WWW_PORT_HTTPS=443

# LetsEncrypt email (recommended for initial issuance)
LETSENCRYPT_EMAIL=admin@example.com